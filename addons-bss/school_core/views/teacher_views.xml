<odoo>
    <record id="view_school_teacher_list" model="ir.ui.view">
        <field name="name">school.teacher.list</field>
        <field name="model">school.teacher</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="employee_code"/>
            </list>
        </field>
    </record>

    <record id="view_school_teacher_form" model="ir.ui.view">
        <field name="name">school.teacher.form</field>
        <field name="model">school.teacher</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="employee_code" invisible="1"/>
                        <field name="login"/>
                        <field name="password"/>
                        <field name="active"/>
                    </group>
                    <notebook>
                        <page string="Subjects">
                            <field name="subject_ids">
                                <list>
                                    <field name="name"/>
                                    <field name="credit"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_school_teachers" model="ir.actions.act_window">
        <field name="name">Teachers</field>
        <field name="res_model">school.teacher</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_school_teachers_action" name="Teachers"
              parent="menu_school_teachers" action="action_school_teachers"
              groups="school_core.group_school_admin,school_core.group_school_teacher"
    />
</odoo>
